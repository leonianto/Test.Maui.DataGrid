<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:dg="clr-namespace:Maui.DataGrid;assembly=Maui.DataGrid"
             x:Class="TestDataGrid.MainPage">
    <Grid RowDefinitions="*" BackgroundColor="White">
        <dg:DataGrid Grid.Row="1" 
                     SelectionMode="Single" 
                     BorderColor="Black"
                     RowHeight="70" 
                     HeaderHeight="50" 
                     HeaderBackground="Beige"
                     HeaderBordersVisible="True"
                     ItemsSource="{Binding List}" 
                     PaginationEnabled="False" 
                     PageSize="5"      
                     ActiveRowColor="Aquamarine"
                     UseAutoColumns="True">
            <dg:DataGrid.Columns>

                <!--<dg:DataGridColumn Title="ID" PropertyName="Id"  />
                <dg:DataGridColumn Title="Name" PropertyName="Name"  />
                <dg:DataGridColumn Title="Surname" PropertyName="Surname"  />
                <dg:DataGridColumn Title="Birthdate" PropertyName="Birthdate"  />
                <dg:DataGridColumn Title="Birthplace" PropertyName="Birthplace"/>-->
            </dg:DataGrid.Columns>

            <dg:DataGrid.RowsBackgroundColorPalette>
                <dg:PaletteCollection>
                    <Color>#F2F2F2</Color>
                    <Color>#FFFFFF</Color>
                </dg:PaletteCollection>
            </dg:DataGrid.RowsBackgroundColorPalette>
        </dg:DataGrid>
    </Grid>
    
</ContentPage>
