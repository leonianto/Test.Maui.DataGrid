<?xml version="1.0" encoding="utf-8" ?>
<Grid xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.DataGrid.CustomStepper" x:Name="self">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="auto"></ColumnDefinition>
        <ColumnDefinition Width="*"></ColumnDefinition>
        <ColumnDefinition Width="auto"></ColumnDefinition>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition Height="auto"></RowDefinition>
    </Grid.RowDefinitions>

    <Button x:Name="removeBtn" Grid.Column="0" Grid.Row="0" Margin="5" WidthRequest="10" HeightRequest="10" Text="-" Clicked="_RemoveBtn_Clicked"></Button>

    <Entry x:Name="entryWidth" Grid.Column="1" Grid.Row="0" Margin="2" Text="{Binding Value, Source={Reference self}, Mode=TwoWay}" VerticalOptions="Center" HorizontalOptions="Center" FontSize="16" Unfocused="_EntryUnfocus"></Entry>

    <Button x:Name="addBtn" Grid.Column="2" Grid.Row="0" Margin="5" WidthRequest="10" HeightRequest="10" Text="+" Clicked="_AddBtn_Clicked"></Button>

</Grid>
